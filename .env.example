# OpenClaw Launcher Config
# Copy to .env and customize

# Optional: basic auth password (leave blank to disable)
LAUNCHER_PASSWORD=

# Max concurrent instances (default: 20)
MAX_INSTANCES=20

# Port (default: 8780)
PORT=8780

# Tailscale IP for Docker port binding
TAILSCALE_IP=100.118.141.107
